# Copy this file to .env.local and fill in your keys.
# .env.local is git-ignored — never commit real secrets.

# Anthropic Claude API — used by /api/analyze
ANTHROPIC_API_KEY=

# newsdata.io free tier — used by /api/news
NEWSDATA_API_KEY=
