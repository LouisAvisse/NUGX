// lib/session.ts — trading session detection.
// Will export a function that takes a Date (or uses now()) and
// returns a TradingSession (see lib/types.ts) based on UTC hour
// per the table in .claude/context.md > Session logic.
// Placeholder until the logic lands in a later commit.

export {}
