// useAnalysis — manual + auto-trigger (every 30 min) /api/analyze caller.
// Will expose { result, loading, run(), nextRunIn } so AnalysisPanel
// can render the result, the analyze button, and the countdown.
// Placeholder for now.

export {}
