// lib/utils.ts — shared formatters and small helpers.
// Will export price/percent/time formatters and the news impact
// tagging logic (HIGH/MEDIUM/LOW keyword matchers from the spec).
// Placeholder until the helpers land in a later commit.

export {}
