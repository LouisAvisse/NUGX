// TradingViewChart — center panel of the dashboard.
// Will embed the TradingView XAUUSD widget (1H interval, dark theme,
// RSI + MACD studies, locale en, timezone Europe/Paris) via iframe.
// Placeholder for now.

export default function TradingViewChart() {
  return <div style={{ color: '#444444', fontSize: '11px' }}>TradingViewChart</div>
}
