// AnalysisPanel — top of the right sidebar.
// Will show Claude's bias / confidence / recommendation, plus
// entry / stop / target / catalyst, with a manual analyze button
// and the auto-trigger countdown. Wired to useAnalysis later.
// Placeholder for now.

export default function AnalysisPanel() {
  return <div style={{ color: '#444444', fontSize: '11px' }}>AnalysisPanel</div>
}
