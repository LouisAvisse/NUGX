// SignalsPanel — middle of the right sidebar.
// Will display DXY and US10Y quotes (price, change, changePct)
// plus derived signals (RSI, trend, volume) once those exist.
// Wired to useSignals later. Placeholder for now.

export default function SignalsPanel() {
  return <div style={{ color: '#444444', fontSize: '11px' }}>SignalsPanel</div>
}
